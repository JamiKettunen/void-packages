# Template file for 'lomiri-url-dispatcher'
pkgname=lomiri-url-dispatcher
version=0.1.1r768
revision=1
_commit=0da1bf83f9abe26e4bf039f61871618fed379c08 # tip of main branch as of 2022-05-31
wrksrc="lomiri-url-dispatcher-${_commit}"
build_style=cmake
configure_args="-DUSE_SYSTEMD=OFF -DENABLE_COVERAGE=OFF"
hostmakedepends="pkg-config glib-devel intltool ubports-cmake-extras"
makedepends="glib-devel lomiri-app-launch-devel mir1-devel protobuf-devel
 libxkbcommon-devel json-glib-devel sqlite-devel
 libapparmor-devel gettext-devel"
checkdepends="gtest-devel dbus-test-runner-devel dbus python3-dbusmock"
short_desc="Lomiri service for requesting URLs to be opened"
maintainer="Jami Kettunen <jami.kettunen@protonmail.com>"
license="LGPL-3.0-only, GPL-3.0-only"
homepage="https://gitlab.com/ubports/core/lomiri-url-dispatcher"
distfiles="https://gitlab.com/ubports/core/lomiri-url-dispatcher/-/archive/${_commit}.tar.gz"
checksum=0732a1b73041b25d388cbf68513cba25685fcff9edee8450ffdca1d052373f32

if [ -z "$XBPS_CHECK_PKGS" ] || [ "$CROSS_BUILD" ]; then
	configure_args+=" -DENABLE_TESTS=OFF"
fi

lomiri-url-dispatcher-devel_package() {
	depends="${sourcepkg}>=${version}_${revision}"
	short_desc+=" - development files"
	pkg_install() {
		vmove usr/include
		vmove usr/lib/pkgconfig
		vmove "usr/lib/*.so"
	}
}

# FIXME: service-test hangs & url-db-test fails!
